{
  "name": "fetch_copilot_usage",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "setup": "bun index.ts setup",
    "usage": "bun index.ts",
    "dev": "bun run --watch index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build ./index.ts --compile --target=bun-darwin-arm64 --outfile copilot-usage --minify",
    "release": "release-it"
  },
  "dependencies": {},
  "devDependencies": {
    "@tsconfig/bun": "latest",
    "@types/bun": "latest",
    "release-it": "latest"
  },
  "version": "0.0.1"
}
